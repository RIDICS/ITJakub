<UserControl
    x:Class="ITJakub.MobileApps.Client.SynchronizedReading.View.ReadingEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:view="using:ITJakub.MobileApps.Client.SynchronizedReading.View"
    xmlns:viewModel="using:ITJakub.MobileApps.Client.SynchronizedReading.ViewModel"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance d:Type=viewModel:ReadingEditorViewModel}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
          Margin="120,0,120,20">
        
        <!--TODO simulation for teacher's view-->
        <ScrollViewer>
            <Grid>
                <view:ReaderRichEditBox SelectionStart="{Binding SelectionStart, Mode=TwoWay}"
                                        SelectionLength="{Binding SelectionLength, Mode=TwoWay}" />
                <!--<Grid Background="#01FFFFFF" />-->
            </Grid>
        </ScrollViewer>

    </Grid>
</UserControl>
