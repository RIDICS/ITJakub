@model ITJakub.Web.Hub.Areas.Admin.Models.UserListViewModel

@if (Model != null && Model.List.Count != 0)
{
    <div class="table-responsive">
        <table class="table table-hover">
            <thead class="thead-light administration-table-head">
                <tr>
                    <th class="align-middle">
                        @Html.DisplayNameFor(model => model.List[0].UserName)
                    </th>
                    <th class="align-middle">
                        @Html.DisplayNameFor(model => model.List[0].FirstName)
                    </th>
                    <th class="align-middle">
                        @Html.DisplayNameFor(model => model.List[0].LastName)
                    </th>
                    <th class="align-middle">
                        @Html.DisplayNameFor(model => model.List[0].Email)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @await Html.PartialAsync("_ListPage", Model)
            </tbody>
        </table>
    </div>
}
else
{
    <div class="alert alert-info">
        <i class="fa fa-info-circle fa-lg"></i>
        @*
            @if (!string.IsNullOrEmpty((string)ViewData["search"]))
            {
                @(Localizer.Translate("no-users-search", "UserViewModel") + "\"")<span class="searched-query">@ViewData["search"]</span>
                @("\"")
            }
            else
            {
                @Localizer.Translate("no-users-found", "UserViewModel")
            }*@
    </div>
}