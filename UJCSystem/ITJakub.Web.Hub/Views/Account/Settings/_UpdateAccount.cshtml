@using ITJakub.Web.Hub.Constants
@using Scalesoft.Localization.AspNetCore
@model ITJakub.Web.Hub.Models.User.UpdateUserViewModel
@inject ILocalizationService Localizer

<div class="row">
    <form asp-controller="Account" asp-action="UpdateAccount" method="post" class="form-horizontal col-xs-12">
        <div class="row">
            <div class="col-md-offset-3 col-md-5">
                <div asp-validation-summary="All" class="alert alert-danger"></div>
                @if (ViewData.TryGetValue(AccountConstants.SuccessUserUpdate, out var success) && (bool)success)
                {
                    <div class="alert alert-success" role="alert">
                        @Localizer.Translate("SuccessUpdateAccount", "Account")
                    </div>
                }
            </div>
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.UserName, new { @class = "col-md-3 control-label" })
            <div class="col-md-5">
                @Html.TextBoxFor(m => m.UserName, new { @class = "form-control", @readonly = true })
            </div>
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.FirstName, new { @class = "col-md-3 control-label" })
            <div class="col-md-5">
                @Html.TextBoxFor(m => m.FirstName, new { @class = "form-control editable", @readonly = true })
            </div>
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.LastName, new { @class = "col-md-3 control-label" })
            <div class="col-md-5">
                @Html.TextBoxFor(m => m.LastName, new { @class = "form-control editable", @readonly = true })
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-3 col-md-5">
                <div id="account-view-button-panel">
                    <button class="btn btn-default" id="account-edit-button">@Localizer.Translate("Edit")</button>
                </div>
                <div id="account-editor-button-panel" class="hide">
                    <button class="btn btn-default" id="account-cancel-button">@Localizer.Translate("Cancel")</button>
                    <input type="submit" class="btn btn-default" value="@Localizer.Translate("UpdateAccountSubmit", "Account")" />
                </div>
            </div>
        </div>
    </form>
</div>