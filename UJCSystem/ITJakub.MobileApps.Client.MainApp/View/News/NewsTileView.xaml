<UserControl
    x:Class="ITJakub.MobileApps.Client.MainApp.View.News.NewsTileView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ITJakub.MobileApps.Client.MainApp.View.News"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:news="using:ITJakub.MobileApps.Client.MainApp.ViewModel.News"
    mc:Ignorable="d"
    d:DesignHeight="400"
    d:DesignWidth="300"
    MinWidth="300"
    MinHeight="400"
    d:DataContext="{d:DesignInstance Type=news:NewsViewModel}"
    DataContext="{Binding NewsViewModel, Source={StaticResource Locator}}">
    <Grid Background="DarkCyan">
        <ListView
            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
            HorizontalContentAlignment="Stretch"
            SelectionMode="Single" ItemsSource="{Binding Path=News}">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Path=.}" TextWrapping="Wrap" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>