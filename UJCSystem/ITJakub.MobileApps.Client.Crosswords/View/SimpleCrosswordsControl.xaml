<UserControl
    x:Class="ITJakub.MobileApps.Client.Crosswords.View.SimpleCrosswordsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:crosswords="using:ITJakub.MobileApps.Client.Crosswords"
    xmlns:view="using:ITJakub.MobileApps.Client.Crosswords.View"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    d:DataContext="{d:DesignInstance d:Type=crosswords:SimpleCrosswordsViewModel}">
    
    <UserControl.Resources>
        <view:StartPositionToMarginConverter x:Key="StartPositionToMarginConverter"/>
        <view:AnswerPositionToBackgroundConverter x:Key="AnswerPositionToBackgroundConverter" />
    </UserControl.Resources>
    
    <ItemsControl ItemsSource="{Binding Crossword}">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Button Padding="0"
                        Margin="0,-3,0,-3"
                        BorderBrush="Transparent"
                        BorderThickness="0"
                        FontWeight="Normal"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Left">
                    <Button.Flyout>
                        <Flyout Placement="Bottom">
                            <TextBox Text="{Binding Word, Mode=TwoWay}"
                                     Width="200"
                                     MaxLength="{Binding Letters.Count}"
                                     KeyUp="WordTextBox_OnKeyUp"/>
                        </Flyout>
                    </Button.Flyout>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                    
                        <TextBlock Grid.Column="0"
                                   FontSize="16"
                                   Margin="20,0,10,0"
                                   TextWrapping="WrapWholeWords"
                                   TextTrimming="CharacterEllipsis"
                                   MaxLines="2"
                                   VerticalAlignment="Center"
                                   Text="{Binding Label}" />
                    
                        <ItemsControl Grid.Column="1"
                                      Margin="{Binding StartPosition, Converter={StaticResource StartPositionToMarginConverter}, ConverterParameter=48}"
                                      ItemsSource="{Binding Letters}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Width="50"
                                            Height="50"
                                            BorderBrush="Black"
                                            BorderThickness="2"
                                            Margin="-1"
                                            Background="{Binding IsPartOfAnswer, Converter={StaticResource AnswerPositionToBackgroundConverter}}">
                                        <TextBlock VerticalAlignment="Center"
                                                   HorizontalAlignment="Center"
                                                   FontSize="20"
                                                   FontFamily="Segoe Print"
                                                   FontWeight="Bold"
                                                   Text="{Binding Letter}" />
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </Button>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
